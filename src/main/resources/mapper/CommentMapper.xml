<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
 
<mapper namespace="kr.co.m2m.instagram.mapper.CommentMapper">
    
    <!-- 좋아요 -->
	<!-- 댓글 추천 시 Like 테이블에 insert -->
	<update id="likesCount">
		update post set like_cnt = like_cnt+1 where post_no = ${post_no};
	</update>
    <select id="commentCount" resultType="int">
        SELECT
            COUNT(*)
        FROM COMMENT
    </select>
    <!--  -->
    <select id="commentList" resultType="CommentVO">
        SELECT
            (comment_no, member_no, post_no, text, REG_DATE)
        FROM COMMENT
    </select>
    <select id="selectComment" resultType="CommentVO">
        SELECT
            (comment_no, member_no, post_no, text, REG_DATE)
        FROM COMMENT WHERE post_id = ${postId}
    </select>
    <insert id="insertComment" parameterType="CommentPO">
        INSERT INTO
        COMMENT (comment_no, member_no, post_no, text, REG_DATE)
        VALUES(${commentId},${memberId},${postId},#{text},#{deleteYN},now()) 
    </insert>
    <update id="updateComment" parameterType="CommentPO">
        UPDATE COMMENT
            SET
            text = #{text}
        	WHERE comment_no = ${commentId}
    </update>
    <update id="deleteComment" parameterType="CommentPO">
	    UPDATE COMMENT
	        SET
	        delete_yn = 'Y'
	    	WHERE comment_no = ${comment_no}
    </update>
</mapper>
