<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.m2m.instagram.member.mapper.MemberMapper">
	
	<!--회원검색  -->
	<select id="selectAllMembers" resultType="kr.co.m2m.instagram.member.model.MemberVO">
		SELECT * FROM instagram.dbo.member
	</select>
	
	<!--회원가입  -->
	<insert id="insertMember" parameterType="kr.co.m2m.instagram.member.model.MemberVO">
		INSERT INTO instagram.dbo.member (login_name, user_name, email, password, gender, delete_yn, media_id)
		VALUES (#{login_name}, #{user_name}, #{email}, #{password}, #{gender}, 'Y', #{media_id})
	</insert>
	
	<!--로그인 -->
	<select id="login_check" resultType="kr.co.m2m.instagram.member.model.MemberVO">
	  SELECT *
	  FROM instagram.dbo.member
	  WHERE email=#{email} and password=#{password}
	 </select>
	
</mapper>