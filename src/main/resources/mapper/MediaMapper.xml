<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.m2m.instagram.media.mapper.MediaMapper">
	<insert id="insertMedia" parameterType="MediaVO" useGeneratedKeys="true" keyProperty="mediaNo">
		INSERT INTO media (filename)
		VALUES (#{filename})
	</insert>
	<update id="updateMedia" parameterType="MediaVO">
		UPDATE media
		SET filename = #{filename}
		WHERE media_no = #{mediaNo}
	</update>
	<update id="deleteMedia" parameterType="Integer">
		UPDATE media
		SET delete_yn = 'Y'
		WHERE media_no = #{mediaNo}
	</update>
	<select id="selectMedia" resultType="MediaVO">
		SELECT * FROM media
		WHERE media_no = #{mediaNo}
	</select>
</mapper>

